import { SearchSuggestionContentDirective } from './search-suggestion-content.directive';

describe('SearchSuggestionContentDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchSuggestionContentDirective();
    expect(directive).toBeTruthy();
  });
});
